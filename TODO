TODO


+ validate member flags for KybJob
+ Unify method jobs
+ add queue to local multithreading
+ Replace threading with multiprocessing (threading doesn't really
work because of Global Intepreter Lock in python.
http://www.python.org/dev/peps/pep-0371/
+ hold interface
+ Need a better way to set job and file names. Preferably human
readable and unique.
+ Clear memory after pickling in the constructor, so reduce memory
usage
