TODO


+ validate member flags for KybJob
+ Unify method jobs
+ Solve issue with environment variable LD_LIBRARY_PATH (i.e. import
DRMAA on cluster
+ add queue to local multithreading
+ hold interface
+ Need a better way to set job and file names. Preferably human
readable and unique.
+ Set additional path from a config file, self.pythonpath.extend(additionalpaths)
